import { Logo } from "../Logo";

export const Header = () => {
    return (
        <header className="h-[12.5rem] bg-gray-700 flex items-center justify-center">
            <Logo />
        </header>
    );
};
